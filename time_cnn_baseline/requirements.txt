# Minimal baseline requirements
pandas>=2.0
numpy>=1.26
pillow>=10.0
tqdm>=4.66
scikit-learn>=1.3
matplotlib>=3.8
# Install torch/torchvision appropriate for your platform (CUDA/MPS/CPU)
# Example (CUDA): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Example (Mac MPS): pip install torch torchvision
